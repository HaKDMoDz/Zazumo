using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Phat
{
    public interface IGameSetters
    {
        void SetGameMode(GameMode gameMode);
        void SetActorFactory(IActorFactory actorFactory);
        void SetBus(IBus bus);
        void SetProcessRunner(IProcessRunner processRunner);
        void SetRepository(IActorRepository actorRepository);
        void SetResourceDictionary(IResourceDictionary resourceDictionary);
        void SetViewPort(ViewPort viewPort);
        void SetSaveData(Object saveData);

        void SetGameModeStack(GameStack gameStack);
    }
}
