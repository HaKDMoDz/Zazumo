using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using FarseerPhysics.Dynamics;

namespace Phat.Physics
{
    public interface IActorPhysics
    {
        void HandleEvent(Object @event);
        Body CreateBody(World world);
        void RemoveBodies(World world);
        void Initialize(Object initializationData);
    }
}
