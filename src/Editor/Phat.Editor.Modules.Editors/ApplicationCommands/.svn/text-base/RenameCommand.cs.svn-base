using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.ComponentModel.Composition;
using Microsoft.Practices.Prism.Events;
using System.Windows.Input;
using Microsoft.Practices.Prism.Commands;
using Phat.Editor.Interfaces.Events;
using Phat.Editor.Interfaces.DatabaseModel;

namespace Phat.Editor.Modules.Editors.ApplicationCommands
{
/*    [Export]
    [PartCreationPolicy(CreationPolicy.Shared)]
    public class RenameCommand
    {
        private readonly IEventAggregator _eventAggregator;
        public ICommand Command { get; private set; }

        [ImportingConstructor]
        public RenameCommand(IEventAggregator eventAggregator)
        {
            this._eventAggregator = eventAggregator;
            this.Command = new DelegateCommand<Asset>(x =>
                {
                    _eventAggregator.GetEvent<CompositePresentationEvent<AssetRenamedEvent>>()
                        .Publish( new AssetRenamedEvent(x.Id, 
                });
        }
    }*/
}
